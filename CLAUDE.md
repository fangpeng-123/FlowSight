# FlowSight

## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/<effort>/` (local-markdown tracker): decision tickets in `issues/`, build tickets in `tickets/`, specs under `docs/spec/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root, created lazily by `/domain-modeling`. See `docs/agents/domain.md`.
